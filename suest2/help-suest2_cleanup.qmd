---
title: "suest2_cleanup help file"
subtitle: "The installed help file, rendered for the web"
toc-depth: 3
---

::: {.callout-note appearance="simple"}
This page is generated from `suest2_cleanup.sthlp` by `build.do`, so it matches the
installed version. In Stata, type `help suest2_cleanup`.
:::

## Title

**suest2_cleanup** list or remove private resources created by `suest2`

## Syntax

`suest2_cleanup` [`,` `force`]

## Description

`suest2_cleanup` lists private stored estimates used by the active-system prediction dispatcher and any hidden composite pweight variables retained so that official `margins` can weight heterogeneous-sample systems. Cleanup uses explicit ownership metadata; user estimates or variables are never removed merely because their names begin with `__s2_` or `__s2pw`.

Specify `force` only after active and stored `suest2` results that depend on these resources are no longer needed.

Each `suest2` run leaves its private constituent copies stored (two per run, more under `mi`), and Stata caps stored estimation sets at 300, so sessions with very many runs eventually fail with *unable to preserve* *constituent model*. Calling `suest2_cleanup, force` periodically reclaims all of it while preserving user-stored estimates. `mecompare` does this automatically at the start of each of its own runs.
